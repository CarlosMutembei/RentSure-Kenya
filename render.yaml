services:
  - type: web
    name: rentsure-kenya
    env: python
    plan: free
    # 1. Point this directly to your clean pip-based build script
    buildCommand: ./build.sh
    # 2. Replaced 'poetry run' with direct gunicorn execution
    startCommand: gunicorn rentsure.wsgi:application
    
    # 3. Render installs these system dependencies BEFORE running your build script
    packages:
      - gdal-bin
      - libgdal-dev
      - binutils
      - libproj-dev
      - python3-dev
      - build-essential

    envVars:
      - key: PYTHON_VERSION
        value: "3.12.3"  # <-- Forces Render to use a stable, production-ready environment
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: RENDER
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: rentsure-db
          property: connectionString
      - key: GDAL_LIBRARY_PATH
        value: /usr/lib/x86_64-linux-gnu/libgdal.so
      - key: GDAL_DATA
        value: /usr/share/gdal
      - key: PROJ_LIB
        value: /usr/share/proj
      - key: PYTHONUNBUFFERED
        value: "true"
      - key: PIP_DEFAULT_TIMEOUT
        value: "100"

databases:
  - name: rentsure-db
    plan: free
    databaseName: rentsure_kenya
    user: rentsure_user